services:
  app:
    image: node:24-alpine
    working_dir: /app
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: development
    volumes:
      - type: bind
        source: .
        target: /app
      - /app/node_modules
    command: sh -c "npm install && npm run dev"
